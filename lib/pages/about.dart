import 'package:flutter/material.dart';

class About extends StatelessWidget {
  final newCookies;
  const About({super.key, required this.newCookies});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
